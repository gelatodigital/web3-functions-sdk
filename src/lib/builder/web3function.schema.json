{
  "title": "Web3 Function Schema",
  "description": "Web3 Function Schema",
  "type": "object",
  "properties": {
    "web3FunctionVersion": {
      "enum": ["1.0.0"]
    },
    "runtime": {
      "enum": ["js-1.0"]
    },
    "memory": {
      "enum": [128, 256]
    },
    "timeout": {
      "type": "number",
      "minimum": 5,
      "maximum": 30
    },
    "userArgs": {
      "type": "object",
      "patternProperties": {
        "^[\\w\\-]+$": {
          "enum": [
            "string",
            "number",
            "boolean",
            "string[]",
            "number[]",
            "boolean[]"
          ]
        }
      },
      "additionalProperties": false
    }
  },
  "required": [
    "web3FunctionVersion",
    "runtime",
    "memory",
    "timeout",
    "userArgs"
  ]
}
